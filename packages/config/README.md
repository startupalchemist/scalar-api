# Config Package

This package will hold client-specific config instead of duplicating backend logic across repos.

Each client config should describe:

- brand identity
- enabled modules
- lead statuses
- public routes
- service definitions
- SEO defaults
- email defaults
